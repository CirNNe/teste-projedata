spring:
  application:
    name: Teste Pratico Projedata
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:teste
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
